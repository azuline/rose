# Task 065: Rules — Per-Field Matching Integration Tests (T-8.1)

## Objective
Add Rust integration tests that exercise the full rules pipeline (FTS search → filter → action → flush → verify) for each individual field type: tracktitle, releasetitle, releasedate, releasetype, tracknumber, discnumber, genre, label, releaseartist, trackartist.

## Context
Python has 18 field-specific tests (`test_rules_fields_match_*`) that each run a rule through the complete pipeline with real audio files and verify the modification on disk. The Rust rules module has unit tests for `matches_pattern` and action execution but zero integration tests exercising the full pipeline.

## Files to Read
- `rose-rs/rose-core/src/rules.rs` — Rust implementation + tests (tests start ~line 1996)
- `rose-py/rose/rules_test.py` — Python tests: `test_rules_fields_match_*` (tests 7-20)

## What to Implement

Add the following integration tests to `rose-rs/rose-core/src/rules.rs` (requires seeded DB + audio fixtures):

1. **`test_pipeline_match_tracktitle`** — Rule: `tracktitle:Track 1` + action `replace:New Title`. Execute. Verify track title changed on disk.

2. **`test_pipeline_match_releasetitle`** — Rule: `releasetitle:Release 1` + action `replace:New Album`. Execute. Verify.

3. **`test_pipeline_match_releasedate`** — Rule: `releasedate:2023` + action on a different tag. Verify the match worked.

4. **`test_pipeline_match_releasetype`** — Rule: `releasetype:album` + action `replace:ep`. Execute. Verify.

5. **`test_pipeline_match_tracknumber`** — Rule: `tracknumber:1` + action on tracktitle. Verify.

6. **`test_pipeline_match_genre`** — Rule: `genre:^Deep House$` + action `replace:Techno`. Verify genre changed.

7. **`test_pipeline_match_label`** — Rule: `label:^Native State$` + action `replace:New Label`. Verify.

8. **`test_pipeline_match_releaseartist`** — Rule: `releaseartist:Techno Man` + action on releasetitle. Verify match.

9. **`test_pipeline_match_trackartist`** — Rule: `trackartist:Techno Man` + action on tracktitle. Verify match.

10. **`test_pipeline_match_new`** — Rule: `new:true` + action `replace:false`. Verify `new` toggled in sidecar file.

11. **`test_pipeline_match_favorite`** — Rule: `favorite:false` + action `replace:true`. Verify `favorite` toggled.

## Tests to Write
11 new test functions.

## Done When
- All 11 tests pass with `cargo test -p rose-core rules`
- `cargo clippy -p rose-core` is clean
- Commit the changes with a descriptive message

## Handoff
**When this task is complete, rename this file from `065.md` to `065.completed.md`.**
Append `## Completed` section.

## Completed

All 11 per-field matching integration tests implemented in `rose-rs/rose-core/src/rules.rs`:

1. `test_pipeline_match_tracktitle` — matches on tracktitle, replaces it
2. `test_pipeline_match_releasetitle` — matches on releasetitle, replaces it
3. `test_pipeline_match_releasedate` — matches on releasedate, verifies via tracktitle action
4. `test_pipeline_match_releasetype` — matches on releasetype, replaces to "ep"
5. `test_pipeline_match_tracknumber` — matches on tracknumber, verifies via tracktitle action
6. `test_pipeline_match_genre` — matches on genre with exact anchors, replaces
7. `test_pipeline_match_label` — matches on label with exact anchors, replaces
8. `test_pipeline_match_releaseartist` — matches on releaseartist, verifies via releasetitle action
9. `test_pipeline_match_trackartist` — matches on trackartist, verifies via tracktitle action
10. `test_pipeline_match_new` — matches new:true, toggles to false in sidecar
11. `test_pipeline_match_favorite` — matches favorite:false, toggles to true in sidecar

Each test uses the full pipeline: copies real `.m4a` audio files from `testdata/`, runs `update_cache()` to populate DB + FTS index, executes the rule via `execute_metadata_rule_inner`, and reads back tags/sidecar to verify changes.

- All 47 rules tests pass (36 existing + 11 new)
- `cargo clippy -p rose-core` clean (no new warnings)
- Committed as `aeabb60`
