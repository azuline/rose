[package]
name = "rose-py"
version = "0.1.0"
edition = "2021"

[lib]
name = "rose"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
rose-core = { path = "../rose-core" }
